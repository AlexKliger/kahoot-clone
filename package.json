{
  "scripts": {
    "start": "cd server && node server.js",
    "dev": "cd server && nodemon server.js --ignore client",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "connect-mongo": "^3.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mathjs": "^11.2.1",
    "mongoose": "^6.7.2",
    "passport-local": "^1.0.0",
    "socket.io": "^4.5.3",
    "uid": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
